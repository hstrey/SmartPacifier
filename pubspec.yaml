name: smart_pacifier
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0-272.0.dev <4.0.0'

dependencies:
  syncfusion_flutter_charts: ^23.1.41
  flutter_reactive_ble: ^5.2.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/images/pacifier1.png
  uses-material-design: true
